<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="productdata.list">
      <field name="name">productdata list</field>
      <field name="model">productdata.productdata</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="productdata.action_window">
      <field name="name">productdata window</field>
      <field name="res_model">productdata.productdata</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="productdata.action_server">
      <field name="name">productdata server</field>
      <field name="model_id" ref="model_productdata_productdata"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="productdata" id="productdata.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="productdata.menu_1" parent="productdata.menu_root"/>
    <menuitem name="Menu 2" id="productdata.menu_2" parent="productdata.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="productdata.menu_1_list" parent="productdata.menu_1"
              action="productdata.action_window"/>
    <menuitem name="Server to list" id="productdata" parent="productdata.menu_2"
              action="productdata.action_server"/>
    -->
  </data>
</odoo>